# My Portfolio

This is a React portfolio created with create-react-app.

## Scripts

- Install: `npm install`
- Start dev server: `npm start`
- Run tests: `npm test`
- Build for production: `npm run build`

## Notes

This repository was set up from an existing local project and pushed to https://github.com/TooKhush/Portfolio.git
